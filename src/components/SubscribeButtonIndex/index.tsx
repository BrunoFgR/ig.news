import styles from './styles.module.scss';

export function SubscribeButtonIndex() {
  return (
    <button
      type="button"
      className={styles.subscribeButton}>
        Subscribe now
      </button>
  )
}